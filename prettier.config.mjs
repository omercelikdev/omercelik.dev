/** Prettier defaults — double quotes, semicolons, trailing commas, 80
 *  columns: the style the codebase was already written in. Run
 *  `npm run format`; CI runs `npm run format:check`. */
const config = {};

export default config;
